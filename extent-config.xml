<?xml version="1.0" encoding="UTF-8"?>
<extentreports>
    <configuration>

        <!-- report theme -->
        <!-- STANDARD, DARK -->
        <theme>dark</theme>

        <!-- document encoding -->
        <!-- defaults to UTF-8 -->
        <encoding>UTF-8</encoding>

        <!-- protocol for script and stylesheets -->
        <!-- defaults to https -->
        <!-- HTTP, HTTPS -->
        <protocol>https</protocol>

        <!-- offline report -->
        <!--<timelineEnabled>true</timelineEnabled>-->

        <!-- offline report -->
        <!--<enableOfflineMode>false</enableOfflineMode>-->

        <!-- use thumbnails for base64 images -->
        <!-- this may slowdown viewing tests -->
        <!--<thumbnailForBase64>false</thumbnailForBase64>-->

        <!-- title of the document -->
        <documentTitle>ExtentReports 4.0</documentTitle>

        <!-- report name - displayed at top-nav -->
        <reportName></reportName>

        <reportHeadline>Od laika do Automatyka - Raport z testów</reportHeadline>

        <!-- timestamp format -->
        <!--<timeStampFormat>MMM dd, yyyy HH:mm:ss</timeStampFormat>-->

        <dateFormat>yyyy-MM-dd</dateFormat>

        <timeFormat>HH:mm:ss</timeFormat>

        <!-- custom javascript -->
        <scripts>
            <![CDATA[
            $(document).ready(function() {

            });
            ]]>
        </scripts>

        <!-- custom styles -->
        <styles>
            <![CDATA[

            ]]>
        </styles>

    </configuration>
</extentreports>